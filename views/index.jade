<!DOCTYPE html>
html(lang="en")
    head
        link(rel="shortcut icon", href="http://www.dagondesign.com/tool-data/favicon/1359858661.ico", type="image/x-icon")
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        meta(http-equiv="X-UA-Compatible", content="ie=edge")
        title Treina Web
    body    
    h1 Cursos
    each val in data
        li            
            div=val.nome
            div=val.categoria
            br

    br
    br
    console.log(data)

    form(action = "/" method="post")
        label Nome:
        input(
            type="text"
            name="nome"
            required
        )
        label Categoria:
        select(name="categoria" required) 
            option Back-End
            option Desenvolvimento de Jogos
            option front end
            option mobile
        input(
            type = "submit"
            value = "enviar"
        )